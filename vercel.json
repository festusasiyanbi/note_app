{
	"builds": [
	  {
		"src": "gunicorn --bind=0.0.0.0:$PORT main:app",
		"use": "@vercel/python"
	  }
	],
	"routes": [
	  {
		"src": "/(.*)",
		"dest": "/$1",
		"methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
	  }
	]
  }
  